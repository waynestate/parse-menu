<?php namespace Waynestate\Menuitems;

/**
 * Class ParseMenu
 * @package Waynestate
 */
class ParseMenu {

    /**
     * Parse the menu items array
     *
     * @param array $menu
     * @param array $config
     * @return array
     */
    function parse(array &$menu, $config = array())
    {
        return $menu;
    }
}